const swiper = new Swiper('.swiper', {
    // Default parameters
    slidesPerView: 3,
    spaceBetween: 15, 
    pagination: {
        el: '.swiper-pagination',
        type: 'bullets',
        }
    }
);